# MT-107: File List UI Component

## Description

Build React component to display available files with download and delete actions.

## Acceptance Criteria

- [ ] `FileList` component displays files in table/card layout
- [ ] Shows filename, formatted size, relative timestamp
- [ ] "Download" button per file (triggers file download)
- [ ] "Delete" button per file (removes file instantly)
- [ ] Empty state when no files ("No files uploaded yet")
- [ ] Loading state during initial fetch
- [ ] Responsive layout (works on mobile)
- [ ] Styled with Tailwind CSS

## Technical Notes

### File Size Formatting

```typescript
function formatFileSize(bytes: number): string {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
  if (bytes < 1024 * 1024 * 1024) return `${(bytes / 1024 / 1024).toFixed(1)} MB`;
  return `${(bytes / 1024 / 1024 / 1024).toFixed(1)} GB`;
}
```

### Relative Time

```typescript
function formatRelativeTime(date: Date): string {
  const seconds = Math.floor((new Date().getTime() - date.getTime()) / 1000);
  if (seconds < 60) return 'just now';
  if (seconds < 3600) return `${Math.floor(seconds / 60)} minutes ago`;
  if (seconds < 86400) return `${Math.floor(seconds / 3600)} hours ago`;
  return `${Math.floor(seconds / 86400)} days ago`;
}
```

### Component Structure

```tsx
function FileList({ files, onDelete }) {
  if (files.length === 0) {
    return <div className="text-gray-500">No files uploaded yet</div>;
  }

  return (
    <div className="grid gap-2">
      {files.map(file => (
        <FileItem key={file.id} file={file} onDelete={onDelete} />
      ))}
    </div>
  );
}
```

## Files to Create

- `src/client/components/FileList.tsx` - File list component
- `src/client/components/FileItem.tsx` - Individual file row (optional)
- `src/client/utils/format.ts` - Formatting utilities

## Dependencies

- Depends on: MT-106 (Real-time File Updates)
